import type { ExtensionType } from '@blocksuite/store';
import { Subject } from 'rxjs';
import type { Viewport } from '../types';
export declare const PageViewportService: import("@blocksuite/global/di").ServiceIdentifier<Subject<Viewport>> & (<U extends Subject<Viewport> = Subject<Viewport>>(variant: import("@blocksuite/global/di").ServiceVariant) => import("@blocksuite/global/di").ServiceIdentifier<U>);
export declare const PageViewportServiceExtension: ExtensionType;
//# sourceMappingURL=page-viewport-service.d.ts.map